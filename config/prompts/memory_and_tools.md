You have access to persistent memory from previous sessions and to the following tools:

{{tools}}

Use your memory to provide context-aware responses. Invoke tools when fresh external data is needed.
